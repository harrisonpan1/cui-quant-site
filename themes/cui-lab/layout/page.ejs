<section class="page-hero">
    <div class="container narrow">
      <p class="eyebrow"><%= page.eyebrow || 'Cui Quantitative Finance Lab' %></p>
      <h1><%= page.title %></h1>
      <% if (page.description) { %><p class="lead"><%= page.description %></p><% } %>
    </div>
  </section>
  <section class="container narrow prose">
    <%- page.content %>
  </section>